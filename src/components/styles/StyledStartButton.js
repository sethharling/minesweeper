import styled from 'styled-components';

import resetIcon from '../../img/reset-icon.jpg';

export const StyledStartButton = styled.div`
    width: 32px;
    height: 32px;
    background: url(${resetIcon}) #000;
    background-size: cover;
`;